to do:
domain, host controlla dove stanno. cambiali con os.environ[HOST]
add allowed host in settings.py

find /root/.kube/* -type f -mmin +1 -delete ###delete files older than a minute
use supervisor for 2 processes

python /.../manage--- relative paths lost

